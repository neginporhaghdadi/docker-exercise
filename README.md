# Exercise Docker 🚀

این مخزن برای تمرین Docker، Dockerfile و Docker Compose ایجاد شده است.  
در این پروژه یک وب‌سایت ساده با Nginx داخل یک کانتینر Docker اجرا می‌شود.

---

## 📌 ساختار فایل‌ها

exercise-docker/
│
├── public/
│ ├── index.html
│ └── styles.css
│
├── Dockerfile
└── docker-compose.yml


---

## 📄 توضیحات فایل‌ها

### **1. index.html**
یک صفحه ساده HTML با عنوان و متن نمونه.

### **2. styles.css**
استایل‌های پایه شامل رنگ پس‌زمینه، رنگ متن و فونت.

### **3. Dockerfile**
برای ساخت Image با استفاده از Nginx:

```dockerfile
FROM nginx:alpine
COPY public /usr/share/nginx/html
EXPOSE 80
4. docker-compose.yml

برای اجرای کانتینر با استفاده از Docker Compose:
services:
  exercise-docker:
    build: .
    container_name: exercise-docker
    ports:
      - "8080:80"
    restart: unless-stopped
▶️ نحوه اجرای پروژه
۱) ساخت ایمیج و اجرای کانتینر
docker compose up -d

۲) مشاهده سایت

مرورگر را باز کنید و آدرس زیر را وارد کنید:

http://localhost:8080

🏁 نتیجه

این پروژه یک تمرین پایه‌ای برای یادگیری موارد زیر است:

نوشتن Dockerfile

کار با Nginx در Docker

ساخت کانتینر

استفاده از Docker Compose

اجرای پروژه در پورت دلخواه
